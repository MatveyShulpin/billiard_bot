# Telegram-бот бронирования бильярдных столов

Автоматизированная система онлайн-бронирования бильярдных столов через Telegram с административной панелью.

## 🎯 Возможности

### Для клиентов:
- 📅 Бронирование столов на удобное время
- 🕐 Выбор даты (до 7 дней вперёд)
- ⏱ Выбор длительности (1-4 часа)
- 🎱 Выбор конкретного стола или "любой"
- 📋 Просмотр своих бронирований
- 🗑 Отмена бронирований

### Для администраторов:
- 📊 Просмотр броней на сегодня
- ❌ Отмена любого бронирования
- 🔔 Автоматические уведомления о новых и отменённых бронях

## 🏗️ Структура проекта

```
billiard-bot/
├── bot.py                      # Главный файл запуска
├── config.py                   # Конфигурация
├── requirements.txt            # Зависимости Python
├── .env.example               # Пример переменных окружения
├── Dockerfile                 # Docker образ
├── docker-compose.yml         # Docker Compose конфигурация
├── billiard-bot.service       # systemd сервис
├── database/
│   ├── __init__.py
│   ├── database.py           # Инициализация БД
│   ├── models.py             # Модели данных
│   └── repository.py         # Репозиторий (CRUD операции)
├── states/
│   ├── __init__.py
│   └── booking_states.py     # FSM состояния
├── handlers/
│   ├── __init__.py
│   ├── user_handlers.py      # Обработчики пользователей
│   └── admin_handlers.py     # Обработчики администраторов
├── keyboards/
│   ├── __init__.py
│   └── keyboards.py          # Telegram клавиатуры
├── middlewares/
│   ├── __init__.py
│   └── hold_cleanup.py       # Middleware очистки holds
└── utils/
    ├── __init__.py
    ├── time_utils.py         # Утилиты работы со временем
    └── scheduler.py          # Планировщик задач
```

## ⚙️ Технический стек

- **Python**: 3.11+
- **aiogram**: 3.15.0 (Telegram Bot API)
- **SQLite**: База данных
- **APScheduler**: Планировщик задач
- **Docker**: Контейнеризация

## 📋 Требования

- Python 3.11 или выше
- Docker и Docker Compose (для контейнерного развёртывания)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## 🚀 Установка и запуск

### Вариант 1: Локальный запуск

1. **Клонирование репозитория**:
```bash
git clone <repository_url>
cd billiard-bot
```

2. **Создание виртуального окружения**:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установка зависимостей**:
```bash
pip install -r requirements.txt
```

4. **Настройка окружения**:
```bash
cp .env.example .env
# Отредактируйте .env файл, добавьте BOT_TOKEN и ADMIN_IDS
```

5. **Создание директории для БД**:
```bash
mkdir -p data
```

6. **Запуск бота**:
```bash
python bot.py
```

### Вариант 2: Docker (рекомендуется)

1. **Клонирование репозитория**:
```bash
git clone <repository_url>
cd billiard-bot
```

2. **Настройка окружения**:
```bash
cp .env.example .env
# Отредактируйте .env файл
```

3. **Создание директории для БД**:
```bash
mkdir -p data
```

4. **Сборка и запуск**:
```bash
docker-compose up -d
```

5. **Просмотр логов**:
```bash
docker-compose logs -f
```

6. **Остановка**:
```bash
docker-compose down
```

### Вариант 3: Автозапуск через systemd

1. **Копирование проекта**:
```bash
sudo mkdir -p /opt/billiard-bot
sudo cp -r . /opt/billiard-bot/
cd /opt/billiard-bot
```

2. **Настройка окружения**:
```bash
sudo cp .env.example .env
sudo nano .env  # Отредактируйте файл
```

3. **Создание директории для БД**:
```bash
sudo mkdir -p /opt/billiard-bot/data
```

4. **Установка systemd сервиса**:
```bash
sudo cp billiard-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable billiard-bot
sudo systemctl start billiard-bot
```

5. **Проверка статуса**:
```bash
sudo systemctl status billiard-bot
```

## 🔧 Конфигурация

### Переменные окружения (.env)

```bash
# Токен бота от @BotFather
BOT_TOKEN=your_bot_token_here

# ID администраторов через запятую
# Узнать свой ID можно у @userinfobot
ADMIN_IDS=123456789,987654321

# Путь к базе данных (по умолчанию data/billiard_bot.db)
DB_PATH=data/billiard_bot.db
```

### Бизнес-правила (config.py)

Можно изменить параметры в файле `config.py`:

- `TABLES_COUNT`: Количество столов (по умолчанию 2)
- `BOOKING_STEP_MINUTES`: Шаг бронирования в минутах (60)
- `MAX_BOOKING_DAYS`: Максимальный период бронирования (7 дней)
- `MIN_BOOKING_HOURS`: Минимальная длительность (1 час)
- `MAX_BOOKING_HOURS`: Максимальная длительность (4 часа)
- `HOLD_TIMEOUT_MINUTES`: Время удержания слота (10 минут)

Режим работы:
- **Пн-Чт, Вс**: 16:00 - 02:00
- **Пт-Сб**: 16:00 - 04:00

## 📱 Использование

### Команды пользователя

- `/start` - Запуск бота и главное меню
- "📅 Забронировать стол" - Начать процесс бронирования
- "📋 Мои бронирования" - Просмотр активных броней

### Команды администратора

- `/today` - Список броней на сегодня
- `/cancel <id>` - Отмена брони по ID
- "⚙️ Админ-панель" - Открыть админ-панель

### Процесс бронирования

1. Выбор даты (до 7 дней вперёд)
2. Выбор времени начала
3. Выбор длительности (1-4 часа)
4. Выбор стола или "любой"
5. Ввод контактного телефона
6. Подтверждение брони

⏰ У пользователя есть 10 минут на завершение бронирования после выбора стола.

## 🗄️ База данных

Структура SQLite базы данных:

### Таблица `tables`
- `id` - ID стола
- `name` - Название стола
- `is_active` - Активность стола

### Таблица `bookings`
- `id` - ID бронирования
- `user_id` - Telegram ID пользователя
- `username` - Username пользователя
- `table_id` - ID стола (NULL = любой)
- `start_time` - Время начала
- `end_time` - Время окончания
- `phone` - Контактный телефон
- `created_at` - Время создания брони
- `status` - Статус (active/cancelled)

### Таблица `holds`
- `id` - ID удержания
- `user_id` - Telegram ID пользователя
- `table_id` - ID стола
- `start_time` - Время начала
- `end_time` - Время окончания
- `created_at` - Время создания
- `expires_at` - Время истечения

## 🔒 Защита от конфликтов

Система использует несколько механизмов защиты:

1. **Holds (временные удержания)** - слот блокируется на 10 минут
2. **Транзакции БД** - атомарность операций
3. **Проверки доступности** - перед созданием брони
4. **Автоочистка holds** - каждые 2 минуты
5. **Middleware очистка** - перед каждым запросом

## 🐛 Отладка

### Просмотр логов Docker

```bash
docker-compose logs -f
```

### Просмотр логов systemd

```bash
sudo journalctl -u billiard-bot -f
```

### Проверка БД

```bash
sqlite3 data/billiard_bot.db
sqlite> SELECT * FROM bookings;
sqlite> .quit
```

## 📊 Мониторинг

Бот логирует все важные события:
- Запуск и остановка бота
- Создание и отмена бронирований
- Ошибки при работе с БД
- Ошибки отправки уведомлений

## 🔄 Обновление

```bash
# Остановка бота
docker-compose down

# Обновление кода
git pull

# Пересборка и запуск
docker-compose up -d --build
```

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи
2. Убедитесь, что BOT_TOKEN корректен
3. Проверьте права доступа к директории data/
4. Убедитесь, что порты не заняты

## 📝 Лицензия

Проект разработан для автоматизации бронирования бильярдных столов.

## 🎯 TODO (возможные улучшения)

- [ ] Веб-интерфейс для администраторов
- [ ] Статистика и аналитика броней
- [ ] Система лояльности
- [ ] Интеграция с платёжными системами
- [ ] Напоминания о бронированиях
- [ ] Экспорт отчётов
- [ ] Поддержка нескольких заведений
- [ ] API для интеграции с другими системами